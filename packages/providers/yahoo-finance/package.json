{
  "name": "@sage/provider-yahoo-finance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:live": "vitest run --config vitest.live.config.ts"
  },
  "dependencies": {
    "@sage/core": "workspace:*",
    "@sage/provider-interface": "workspace:*",
    "yahoo-finance2": "catalog:"
  },
  "devDependencies": {
    "@sage/tsconfig": "workspace:*",
    "vitest": "catalog:"
  }
}
