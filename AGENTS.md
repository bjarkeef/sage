# Agent instructions

They live in [`CLAUDE.md`](./CLAUDE.md), in full — design system, verification
gates, test-fixture rules, how to run the app, worktrees. **Read that file
before making changes.**

This file is a pointer rather than a copy on purpose. It used to hold a partial
duplicate, and the copy fell behind: an agent reading it got the design-system
section and none of the rules about what must never go into a test fixture.
